<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wudao.mapper.BodyMetricMapper">

    <select id="selectLatestByStudentId" resultType="com.wudao.entity.BodyMetric">
        SELECT metric_id, student_id, student_name, height_cm, weight_kg, bust_cm, waist_cm, hip_cm, torso_length_cm, shoe_size, measured_date, created_at
        FROM student_body_metric
        WHERE student_id = #{studentId}
        ORDER BY metric_id DESC LIMIT 1
    </select>

    <select id="selectHistoryByStudentId" resultType="com.wudao.entity.BodyMetric">
        SELECT metric_id, student_id, student_name, height_cm, weight_kg, bust_cm, waist_cm, hip_cm, torso_length_cm, shoe_size, measured_date, created_at
        FROM student_body_metric
        WHERE student_id = #{studentId}
        ORDER BY metric_id DESC
    </select>

    <select id="selectAll" resultType="com.wudao.entity.BodyMetric">
        SELECT metric_id, student_id, student_name, height_cm, weight_kg, bust_cm, waist_cm, hip_cm, torso_length_cm, shoe_size, measured_date, created_at
        FROM student_body_metric
        ORDER BY metric_id DESC
    </select>

    <insert id="insert" parameterType="com.wudao.entity.BodyMetric">
        INSERT INTO student_body_metric (
            metric_id, student_id, student_name, height_cm, weight_kg, bust_cm, waist_cm, hip_cm, torso_length_cm, shoe_size, measured_date
        ) VALUES (
            #{metricId}, #{studentId}, #{studentName}, #{heightCm}, #{weightKg}, #{bustCm}, #{waistCm}, #{hipCm}, #{torsoLengthCm}, #{shoeSize}, #{measuredDate}
        )
    </insert>

</mapper>
