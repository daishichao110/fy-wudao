<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wudao.mapper.NoticeMapper">

    <select id="selectAllNotices" resultType="com.wudao.entity.Notice">
        SELECT notice_id, tag, title, content, publisher, created_at
        FROM sys_notice
        ORDER BY notice_id DESC
    </select>

    <insert id="insertNotice" parameterType="com.wudao.entity.Notice">
        INSERT INTO sys_notice (notice_id, tag, title, content, publisher)
        VALUES (#{noticeId}, #{tag}, #{title}, #{content}, #{publisher})
    </insert>

</mapper>
